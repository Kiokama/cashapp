services:
  - type: web
    name: cashapp-backend
    env: node
    plan: free
    buildCommand: npm install --include=dev
    startCommand: npm run server
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
